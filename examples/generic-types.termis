(type Pair (A B)
  (product
    (first A)
    (second B)))

(type CounterPair
  (Pair i32 i64))

(type Identity (T)
  T)

(type CounterId
  (Identity i64))

(fn same-number ((value CounterId)) CounterId
  value)

(fn main () i64
  (same-number 42))
